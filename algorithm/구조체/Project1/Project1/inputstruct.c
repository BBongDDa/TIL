#include <stdio.h>
#include <stdlib.h>
struct student {
	char name[12];
	int age;
	double height;
};

int main() {
	struct student s_list[10];
	for(i)


	for (int i = 0; i <= 6; i++) {
		printf("이름 = %s, 나이 = %d, 키 = %6.2f\n", s_list[i].name, s_list[i].age, s_list[i].height);
	}
	return 0;
}

